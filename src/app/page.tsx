import fetcherApi from "@/helpers/fetcher";


export default async function Home() {
  // const fetcher = fetcherApi()
  // const user = await fetcher.post('user/getUser', {mail: 'feedback@dreamyVerse.com'})
  // console.log(user.data)

  return (
    <main className="">
      <h1>Hola</h1>
    </main>
  );
}
